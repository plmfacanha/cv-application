export default function DisplayCV() {
  return (
    <p
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Im displaying the CV
    </p>
  );
}
